/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // output: 'standalone', // Uncomment if you want standalone output for Docker
}

module.exports = nextConfig
